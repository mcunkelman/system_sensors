"""Sensor variant modules. Each module defines one or more `Sensor` subclasses.

The registry imports this package and walks every submodule to discover variants.
"""

from __future__ import annotations
